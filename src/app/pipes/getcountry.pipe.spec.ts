import { GetcountryPipe } from './getcountry.pipe';

describe('GetcountryPipe', () => {
  it('create an instance', () => {
    const pipe = new GetcountryPipe();
    expect(pipe).toBeTruthy();
  });
});
