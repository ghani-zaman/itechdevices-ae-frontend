import { TrimCatImagePipe } from './trim-cat-image.pipe';

describe('TrimCatImagePipe', () => {
  it('create an instance', () => {
    const pipe = new TrimCatImagePipe();
    expect(pipe).toBeTruthy();
  });
});
